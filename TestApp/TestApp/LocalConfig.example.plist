<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Template for LocalConfig.plist (which is gitignored).
	     Copy this file to LocalConfig.plist and set your own values:

	       cp LocalConfig.example.plist LocalConfig.plist

	     Then in Xcode add LocalConfig.plist to the TestApp target's
	     "Copy Bundle Resources" build phase so it ships in the app bundle.

	     AccountBaseURL: base URL of the local account server.
	     - Simulator: http://localhost:8080 works (host loopback).
	     - Physical device: use the Mac's LAN IP, e.g. http://192.168.1.3:8080
	       (device and Mac must be on the same Wi-Fi). -->
	<key>AccountBaseURL</key>
	<string>http://localhost:8080</string>
</dict>
</plist>
